1. docker container run -d --name=nginx -p 80:80 -v /opt:/usr/share/nginx/html nginx (In linux)
2. docker container run -d --name=nginx -p 80:80 -v $(pwd):/usr/share/nginx/html nginx (In linux to bind mount the current directory) 
3.docker container run -d --name=nginx -p 80:80 -v //c:/user/ravi/documents:/usr/share/nginx/html nginx (In windows)
